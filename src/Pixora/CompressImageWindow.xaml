<Window x:Class="Pixora.CompressImageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="压缩图片"
        Width="700"
        Height="560"
        MinWidth="600"
        MinHeight="440"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}"
        FontFamily="Microsoft YaHei UI"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        TextOptions.TextHintingMode="Fixed"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">
    <Window.Resources>
        <Style x:Key="CardBorder" TargetType="{x:Type Border}">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="16" />
        </Style>

        <Style x:Key="SmallLabel" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
            <Setter Property="FontSize" Value="12" />
        </Style>

        <Style x:Key="MetricValue" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="SliderDecreaseButton" TargetType="{x:Type RepeatButton}">
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Border Height="6"
                                Background="{DynamicResource AccentBrush}"
                                CornerRadius="3"
                                VerticalAlignment="Center" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SliderIncreaseButton" TargetType="{x:Type RepeatButton}">
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Border Height="6"
                                Background="{DynamicResource BorderBrush}"
                                CornerRadius="3"
                                VerticalAlignment="Center" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SliderThumb" TargetType="{x:Type Thumb}">
            <Setter Property="Width" Value="20" />
            <Setter Property="Height" Value="20" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border x:Name="ThumbChrome"
                                Background="{DynamicResource SliderThumbBrush}"
                                BorderBrush="{DynamicResource AccentBrush}"
                                BorderThickness="2"
                                CornerRadius="10" />
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ThumbChrome" Property="Background" Value="{DynamicResource SliderThumbHoverBrush}" />
                                <Setter TargetName="ThumbChrome" Property="BorderBrush" Value="{DynamicResource AccentHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="ThumbChrome" Property="Background" Value="{DynamicResource SliderThumbPressedBrush}" />
                                <Setter TargetName="ThumbChrome" Property="BorderBrush" Value="{DynamicResource AccentHoverBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CompressionSlider" TargetType="{x:Type Slider}">
            <Setter Property="Height" Value="30" />
            <Setter Property="FocusVisualStyle" Value="{DynamicResource KeyboardFocusVisual}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Slider}">
                        <Grid VerticalAlignment="Center">
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.DecreaseLarge}"
                                                  Style="{StaticResource SliderDecreaseButton}" />
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource SliderThumb}" />
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.IncreaseLarge}"
                                                  Style="{StaticResource SliderIncreaseButton}" />
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardBorder}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock x:Name="SourceText"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               FontSize="16"
                               FontWeight="SemiBold"
                               TextTrimming="CharacterEllipsis" />
                    <TextBlock x:Name="DetailText"
                               Margin="0,6,0,0"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               FontSize="13" />
                </StackPanel>
                <Border Grid.Column="1"
                        Background="{DynamicResource AccentSurfaceBrush}"
                        CornerRadius="8"
                        Padding="12,7"
                        VerticalAlignment="Center">
                    <TextBlock x:Name="FormatBadge"
                               Foreground="{DynamicResource AccentHoverBrush}"
                               FontWeight="SemiBold" />
                </Border>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1"
                      Margin="0,14,0,0"
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto">
            <Grid Margin="0,0,4,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.1*" />
                    <ColumnDefinition Width="14" />
                    <ColumnDefinition Width="0.9*" />
                </Grid.ColumnDefinitions>

                <Border Style="{StaticResource CardBorder}">
                    <StackPanel>
                    <TextBlock Text="压缩设置"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               FontSize="15"
                               FontWeight="SemiBold" />

                    <TextBlock Text="格式"
                               Style="{StaticResource SmallLabel}"
                               Margin="0,16,0,7" />
                    <StackPanel Orientation="Horizontal">
                        <RadioButton x:Name="JpegRadio"
                                     Content="JPEG"
                                     IsChecked="True"
                                     Checked="FormatRadio_Checked" />
                        <RadioButton x:Name="PngRadio"
                                     Content="PNG"
                                     Checked="FormatRadio_Checked" />
                    </StackPanel>
                    <TextBlock x:Name="FormatHelpText"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               FontSize="12"
                               TextWrapping="Wrap"
                               Margin="0,8,0,0" />

                    <TextBlock Text="分辨率"
                               Style="{StaticResource SmallLabel}"
                               Margin="0,18,0,7" />
                    <CheckBox x:Name="ResizeCheckBox"
                              Content="调整输出分辨率"
                              Foreground="{DynamicResource TextPrimaryBrush}"
                              Checked="ResizeCheckBox_Changed"
                              Unchecked="ResizeCheckBox_Changed" />
                    <Grid Margin="0,8,0,0"
                          IsEnabled="{Binding IsChecked, ElementName=ResizeCheckBox}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="22" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="96" />
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="WidthText"
                                 Height="34"
                                 Padding="8,0"
                                 Background="{DynamicResource InputBrush}"
                                 BorderBrush="{DynamicResource BorderStrongBrush}"
                                 Foreground="{DynamicResource TextPrimaryBrush}"
                                 TextChanged="SizeTextBox_TextChanged" />
                        <TextBlock Grid.Column="1"
                                   Text="x"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   VerticalAlignment="Center"
                                   TextAlignment="Center" />
                        <TextBox x:Name="HeightText"
                                 Grid.Column="2"
                                 Height="34"
                                 Padding="8,0"
                                 Background="{DynamicResource InputBrush}"
                                 BorderBrush="{DynamicResource BorderStrongBrush}"
                                 Foreground="{DynamicResource TextPrimaryBrush}"
                                 TextChanged="SizeTextBox_TextChanged" />
                        <Button Grid.Column="3"
                                Content="原始尺寸"
                                Margin="8,0,0,0"
                                Click="ResetSizeButton_Click" />
                    </Grid>
                    <CheckBox x:Name="KeepAspectCheckBox"
                              Content="锁定比例"
                              IsChecked="True"
                              Foreground="{DynamicResource TextSecondaryBrush}"
                              Margin="0,8,0,0" />

                    <TextBlock Text="JPEG 质量"
                               Style="{StaticResource SmallLabel}"
                               Margin="0,18,0,4" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="50" />
                        </Grid.ColumnDefinitions>
                        <Slider x:Name="QualitySlider"
                                Minimum="1"
                                Maximum="100"
                                Value="75"
                                TickFrequency="1"
                                IsSnapToTickEnabled="True"
                                Style="{StaticResource CompressionSlider}"
                                ValueChanged="QualitySlider_ValueChanged" />
                        <TextBlock x:Name="QualityText"
                                   Grid.Column="1"
                                   Margin="10,0,0,0"
                                   VerticalAlignment="Center"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   TextAlignment="Right"
                                   FontWeight="SemiBold" />
                    </Grid>

                    <TextBlock Text="输出文件"
                               Style="{StaticResource SmallLabel}"
                               Margin="0,18,0,7" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="88" />
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="OutputPathText"
                                 Height="36"
                                 Padding="9,0"
                                 Background="{DynamicResource InputBrush}"
                                 BorderBrush="{DynamicResource BorderStrongBrush}"
                                 Foreground="{DynamicResource TextPrimaryBrush}"
                                 TextChanged="OutputPathText_TextChanged" />
                        <Button Grid.Column="1"
                                Content="浏览..."
                                Margin="8,0,0,0"
                                Click="BrowseButton_Click" />
                    </Grid>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2"
                        Style="{StaticResource CardBorder}">
                    <StackPanel>
                    <TextBlock Text="预计输出"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               FontSize="15"
                               FontWeight="SemiBold" />

                    <Grid Margin="0,16,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="分辨率"
                                       Style="{StaticResource SmallLabel}" />
                            <TextBlock x:Name="OutputResolutionText"
                                       Style="{StaticResource MetricValue}"
                                       Margin="0,4,0,0" />
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="格式"
                                       Style="{StaticResource SmallLabel}" />
                            <TextBlock x:Name="OutputFormatText"
                                       Style="{StaticResource MetricValue}"
                                       Margin="0,4,0,0" />
                        </StackPanel>
                    </Grid>

                    <Border Background="{DynamicResource SurfaceRaisedBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="14"
                            Margin="0,18,0,0">
                        <StackPanel>
                            <TextBlock Text="预计大小"
                                       Style="{StaticResource SmallLabel}" />
                            <TextBlock x:Name="EstimatedSizeText"
                                       Foreground="{DynamicResource AccentBrush}"
                                       FontSize="28"
                                       FontWeight="SemiBold"
                                       Margin="0,3,0,0" />
                            <TextBlock x:Name="CompressionRatioText"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       FontSize="13"
                                       Margin="0,4,0,0" />
                        </StackPanel>
                    </Border>

                    <TextBlock x:Name="EstimateStatusText"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               FontSize="12"
                               TextWrapping="Wrap"
                               Margin="0,14,0,0" />
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="压缩保存"
                    MinWidth="104"
                    Margin="0,0,8,0"
                    Click="SaveButton_Click" />
            <Button Content="取消"
                    MinWidth="84"
                    Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>
